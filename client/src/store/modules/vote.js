const state = {
    voting: null,
};

const getters = {};

const actions = {};

const mutations = {
    setVoting: (state, voting) => (state.voting = voting),
};

export default {
    state,
    getters,
    actions,
    mutations,
};
